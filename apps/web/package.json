{
  "scripts": {
    "build": "npx tsc",
    "serve": "python3 -m http.server 5173 --directory public",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
